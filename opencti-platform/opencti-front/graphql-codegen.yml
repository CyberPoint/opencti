overwrite: true
schema: "../opencti-graphql/config/schema/opencti.graphql"
documents: null
generates:
  src/generated/graphql.tsx:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useTypeImports: true
  ./graphql.schema.json:
    plugins:
      - introspection
